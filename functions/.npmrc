# store-dir: usar store global para no crear .pnpm-store en la raíz del repo
store-dir=~/.pnpm-store

# node-linker=hoisted: crea node_modules plano (como npm) para que tsc y otros
# binarios resuelvan bien. Evita "Cannot find module .../typescript/bin/tsc".
node-linker=hoisted
